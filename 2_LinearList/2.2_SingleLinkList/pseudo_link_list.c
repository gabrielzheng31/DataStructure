InitFromHead()
{
	operate LNode *p;
	operate header node link_list;
}

InitFromTail()
{
	assign link_list to LNode *tail;
	while(1)
	{
		operate new LNode *p;
		operate *tail;
	}
	tail->next = NULL;
}

GetElemByIndex()
{
    check if index < 1;
    assign header to  LNode *p;
    traverse the list, run to pi;
    if out of boundary return error;
}

GetElemByValue()
{
    assign 
}


